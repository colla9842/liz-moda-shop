/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['testing-uozy.onrender.com'],
      domains: ['res.cloudinary.com'],
    },
  }
  

export default nextConfig;
